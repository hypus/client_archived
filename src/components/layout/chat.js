// import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

function Chat() {
  return <div>Chat</div>;
}

Chat.propTypes = propTypes;
Chat.defaultProps = defaultProps;

export default Chat;
